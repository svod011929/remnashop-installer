# Setup.md - Детальное руководство по установке

## Содержание
1. [Предварительные требования](#предварительные-требования)
2. [Подготовка VPS](#подготовка-vps)
3. [Быстрая установка](#быстрая-установка)
4. [Пошаговая установка](#пошаговая-установка)
5. [Постановка настройка](#постановка-настройка)
6. [Проверка работоспособности](#проверка-работоспособности)

## Предварительные требования

### Что вам нужно
- VPS/Dedicated сервер с Ubuntu 20.04 или выше
- Доменное имя (зарегистрировано и указано на ваш IP)
- SSH доступ к серверу
- Telegram аккаунт
- Мозг и 30 минут времени 😄

### Подготовка доменного имени

Перед установкой убедитесь, что ваш домен правильно указан:

```bash
# Проверьте, что домен разрешается в ваш IP
nslookup example.com

# Должно вывести что-то вроде:
# Non-authoritative answer:
# Name:   example.com
# Address: YOUR_SERVER_IP
```

Если домен не разрешается, обновите DNS записи в панели управления вашего хостинга.

## Подготовка VPS

### Шаг 1: Подключитесь к серверу

```bash
ssh root@your_server_ip
# или если используется непривилегированный пользователь
ssh user@your_server_ip
sudo -i
```

### Шаг 2: Обновите систему (опционально)

```bash
apt update && apt upgrade -y
```

### Шаг 3: Убедитесь, что curl установлен

```bash
curl --version
# Если curl не установлен, установите его
apt install curl -y
```

## Быстрая установка

### Одна строка для установки

```bash
curl -sSL https://raw.githubusercontent.com/svod011929/remnashop-installer/main/install.sh | sudo bash
```

**ИЛИ**

```bash
wget -qO- https://raw.githubusercontent.com/svod011929/remnashop-installer/main/install.sh | sudo bash
```

## Пошаговая установка

### Шаг 1: Загрузите скрипт

```bash
cd /tmp
wget https://raw.githubusercontent.com/svod011929/remnashop-installer/main/install.sh
chmod +x install.sh
```

### Шаг 2: Запустите скрипт

```bash
sudo ./install.sh
```

### Шаг 3: Следуйте интерактивным подсказкам

Скрипт попросит вас ввести:

#### 1️⃣ Доменное имя
```
═══════════════════════════════════════════════════════
Введите доменное имя для вашего сервера
Пример: example.com
═══════════════════════════════════════════════════════
Домен: example.com
```

#### 2️⃣ Telegram Bot Token

Получите токен:
1. Откройте Telegram и найдите [@BotFather](https://t.me/BotFather)
2. Отправьте `/newbot`
3. Следуйте инструкциям
4. Скопируйте полученный токен

```
═══════════════════════════════════════════════════════
Введите токен Telegram бота
Получить можно у @BotFather в Telegram
Формат: 1234567890:ABCDefGhIjKlMnOpQrStUvWxYz
═══════════════════════════════════════════════════════
BOT_TOKEN: 1234567890:ABCDefGhIjKlMnOpQrStUvWxYz
```

#### 3️⃣ Telegram Dev ID

Узнайте свой ID:
1. Найдите [@userinfobot](https://t.me/userinfobot)
2. Отправьте `/start`
3. Скопируйте ваш ID

```
═══════════════════════════════════════════════════════
Введите Telegram ID разработчика для управления ботом
Узнать свой ID можно у @userinfobot
═══════════════════════════════════════════════════════
DEV_ID: 123456789
```

#### 4️⃣ Telegram Support Username

```
═══════════════════════════════════════════════════════
Введите Telegram имя пользователя поддержки
Формат: поддержка (без @)
═══════════════════════════════════════════════════════
SUPPORT_USERNAME: support_team
```

## Постовые настройки

### Шаг 1: Отредактируйте конфигурацию

После установки нужно добавить интеграцию с Remnawave:

```bash
sudo nano /opt/remnashop/.env
```

Найдите и заполните эти строки:

```env
# Ваша панель Remnawave должна быть установлена
# Получите эти значения из панели
REMNAWAVE_TOKEN=your_api_token_here
REMNAWAVE_WEBHOOK_SECRET=your_webhook_secret_here
REMNAWAVE_HOST=your_panel_domain_or_ip
```

### Шаг 2: Перезагрузите контейнеры

```bash
cd /opt/remnashop
docker-compose restart
```

### Шаг 3: Проверьте логи

```bash
docker-compose logs -f bot
```

## Проверка работоспособности

### Команда 1: Статус контейнеров

```bash
cd /opt/remnashop
docker-compose ps

# Результат должен быть:
# NAME                COMMAND                  SERVICE             STATUS              PORTS
# remnashop_bot       "/bin/sh -c 'python"     bot                 Up 2 minutes        127.0.0.1:5000->5000/tcp
# remnashop_db        "docker-entrypoint..."   postgres            Up 2 minutes        127.0.0.1:5432->5432/tcp
# remnashop_redis     "redis-server --re..."   redis               Up 2 minutes        127.0.0.1:6379->6379/tcp
```

### Команда 2: Проверка HTTPS

```bash
curl -I https://your-domain.com

# Должно вывести 200 OK
# HTTP/2 200
```

### Команда 3: Проверка бота

1. Найдите вашего бота в Telegram
2. Отправьте `/start`
3. Бот должен ответить

### Команда 4: Просмотр логов

```bash
# Логи бота
docker-compose logs -f bot

# Логи базы данных
docker-compose logs -f postgres

# Логи Redis
docker-compose logs -f redis

# Все логи
docker-compose logs -f
```

## Решение проблем при установке

### Проблема: SSL сертификат не получен

```
ERROR: Ошибка при получении SSL сертификата
```

**Решение:**
1. Убедитесь, что домен правильно указан: `nslookup your-domain.com`
2. Убедитесь, что порт 80 открыт: `sudo ufw allow 80`
3. Подождите 5-10 минут и запустите повторно

### Проблема: Docker контейнеры не запускаются

```
docker: command not found
```

**Решение:**
- Docker может быть не установлен. Убедитесь, что скрипт завершился успешно.
- Перезагрузитесь: `sudo reboot`

### Проблема: Недостаточно памяти

```
ERROR: Cannot allocate memory
```

**Решение:**
- Используйте более мощный VPS
- Остановите другие сервисы

### Проблема: Порт 443 занят

```
bind: permission denied
```

**Решение:**
```bash
sudo lsof -i :443  # Найдите процесс
sudo kill -9 <PID>  # Остановите его
sudo systemctl restart nginx
```

## Дальнейшие шаги

### 1. Настройка Remnawave

Если у вас еще нет Remnawave, установите его отдельно:

```bash
# Создайте директорию
mkdir /opt/remnawave && cd /opt/remnawave

# Загрузите конфигурацию
wget https://raw.githubusercontent.com/remnawave/backend/main/docker-compose.yml
wget https://raw.githubusercontent.com/remnawave/backend/main/.env.sample -O .env

# Отредактируйте .env
nano .env

# Запустите
docker-compose up -d
```

### 2. Настройка автоматического обновления

```bash
# Создайте скрипт для обновления
cat > /opt/remnashop/update.sh << 'EOF'
#!/bin/bash
cd /opt/remnashop
docker-compose pull
docker-compose up -d
EOF

chmod +x /opt/remnashop/update.sh

# Добавьте в crontab
sudo crontab -e

# Добавьте строку (каждый день в 3:00):
0 3 * * * /opt/remnashop/update.sh
```

### 3. Настройка резервного копирования БД

```bash
# Создайте директорию для бэкапов
mkdir -p /backups/remnashop

# Создайте скрипт резервного копирования
cat > /opt/remnashop/backup.sh << 'EOF'
#!/bin/bash
BACKUP_DIR="/backups/remnashop"
DATE=$(date +%Y-%m-%d_%H-%M-%S)

docker-compose exec -T postgres pg_dump -U ${DATABASE_USER} ${DATABASE_NAME} > \
    ${BACKUP_DIR}/backup_${DATE}.sql

echo "Бэкап создан: ${BACKUP_DIR}/backup_${DATE}.sql"
EOF

chmod +x /opt/remnashop/backup.sh

# Запустите вручную
/opt/remnashop/backup.sh
```

---

**Поздравляем! Ваша установка завершена! 🎉**
